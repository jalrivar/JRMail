@model IEnumerable<JRMail.DAL.MailMessageClassifaction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MailClassification.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MailMessage.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MailClassification.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MailMessage.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MailMessageClassifactionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MailMessageClassifactionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MailMessageClassifactionId })
        </td>
    </tr>
}

</table>
